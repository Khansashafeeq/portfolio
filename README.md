Framer Motion demo scaffold

To run:

```bash
cd framer-portfolio
npm install
npm run dev
```

Opens at the port printed by Vite. This is a small React + Framer Motion preview scaffold; I can port your full site into this structure next.